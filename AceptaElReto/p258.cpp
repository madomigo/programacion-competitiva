#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, k;
    while (true) {
        cin >> n >> k;
        if (n == 0 && k == 0) break;

        for (int i = 0; i < n; i++) {
            
        }
    }
}